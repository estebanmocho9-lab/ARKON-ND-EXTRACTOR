import { sheets, sheetsWrite } from './sheets';
import type { NDMemory } from './neural';
import type { NDFinding } from './types';
let ensured=new Set<string>();
export async function ensureNeuralSheets(id:string){if(ensured.has(id))return;const m=await sheets.spreadsheets.get({spreadsheetId:id,fields:'sheets.properties'});const have=new Set((m.data.sheets||[]).map(s=>s.properties?.title));const names=['MAPA_ND','COORDENADAS_ND','MEMORIA_ND'];const requests=names.filter(n=>!have.has(n)).map(title=>({addSheet:{properties:{title}}}));if(requests.length)await sheets.spreadsheets.batchUpdate({spreadsheetId:id,requestBody:{requests}});await sheets.spreadsheets.values.batchUpdate({spreadsheetId:id,requestBody:{valueInputOption:'RAW',data:[{range:'MAPA_ND!A1:H1',values:[['documento_drive_id','documento','neurona','paginas','hallazgos','coordenadas','resumen_por_tipo','memoria_estado']]},{range:'COORDENADAS_ND!A1:I1',values:[['coordinate_id','documento_drive_id','neurona','pagina','seccion','tipo','entidad','knowledge_id','coordinate_path']]},{range:'MEMORIA_ND!A1:F1',values:[['documento_drive_id','documento','neurona','paginas','hallazgos','estado']]}]}});ensured.add(id);}
export async function appendNeuralChunk(id:string,m:NDMemory,findings:NDFinding[]){await ensureNeuralSheets(id);if(!findings.length)return;const byKind:Record<string,number>={};for(const f of findings)byKind[f.kind]=(byKind[f.kind]||0)+1;const coords=m.coordinates.slice(-findings.length);await sheetsWrite(()=>sheets.spreadsheets.values.append({spreadsheetId:id,range:'MAPA_ND!A1',valueInputOption:'RAW',insertDataOption:'INSERT_ROWS',requestBody:{values:[[m.documentId,m.documentName,m.neuron,[...new Set(findings.map(f=>f.page))].sort((a,b)=>a-b).join(','),findings.length,coords.length,JSON.stringify(byKind),'incremental']]}}));if(coords.length){const rows=coords.map(c=>[c.id,m.documentId,m.neuron,c.page,c.section,c.kind,c.entity,c.knowledgeId,c.path]);await sheetsWrite(()=>sheets.spreadsheets.values.append({spreadsheetId:id,range:'COORDENADAS_ND!A1',valueInputOption:'RAW',insertDataOption:'INSERT_ROWS',requestBody:{values:rows}}));}await sheetsWrite(()=>sheets.spreadsheets.values.append({spreadsheetId:id,range:'MEMORIA_ND!A1',valueInputOption:'RAW',insertDataOption:'INSERT_ROWS',requestBody:{values:[[m.documentId,m.documentName,m.neuron,[...new Set(findings.map(f=>f.page))].sort((a,b)=>a-b).join(','),findings.length,'guardada']]}}));}
export async function appendNeuralMemory(id:string,m:NDMemory){await ensureNeuralSheets(id);const compact:any={};for(const c of m.coordinates)compact[c.kind]=(compact[c.kind]||0)+1;await sheetsWrite(()=>sheets.spreadsheets.values.append({spreadsheetId:id,range:'MAPA_ND!A1',valueInputOption:'RAW',insertDataOption:'INSERT_ROWS',requestBody:{values:[[m.documentId,m.documentName,m.neuron,[...m.pages].sort((a,b)=>a-b).join(','),m.findings,m.coordinates.length,JSON.stringify(compact),'cerrada']]}}));}
